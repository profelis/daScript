Arms the per-lane JobQue event tracer: every worker lane records publish, chunk, stage-wait and wake events into a preallocated buffer of events_per_lane entries; a lane stops recording when its buffer fills. Start (and stop) the trace only while no dispatch is in flight — arming reallocates the lane buffers.
