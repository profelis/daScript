// GENERATED by utils/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl
require strings

var @inout conf_byte3_uv : float2
var @out conf_byte3_color : float4

[fragment_program(name="conf_byte3_glsl")]
def conf_byte3_fragment {
    var v = byte3(1, 2, 3)
    let s = byte3(int(conf_byte3_uv.x))
    let r = v.x
    conf_byte3_color = float4(float(r), 0., 0., 1.)
}

[test]
def test_conf_byte3_glsl(t : T?) {
    t |> run("conformance byte3: emits and validates") <| @(t : T?) {
        t |> success(has(conf_byte3_glsl, "i8vec3"), "spells i8vec3")
        let r = validate_glsl(conf_byte3_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
